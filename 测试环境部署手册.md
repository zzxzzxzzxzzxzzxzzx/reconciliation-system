# 对账系统测试环境部署手册

## 1. 部署目标

将抖店对账和千川消耗系统部署到一台独立测试服务器，供业务同事通过浏览器验收。

测试环境由 4 个 Docker 容器组成：

```text
浏览器 -> Nginx 网关 -> Next.js 前端
                    -> NestJS API -> PostgreSQL
```

- 对外只开放 Nginx 端口，默认 `8080`。
- PostgreSQL 和 API 不直接开放给外部网络。
- 系统使用单管理员账号登录保护所有业务页面和接口。
- API 启动时自动执行已提交的 Prisma 数据库迁移。
- 测试环境默认使用空数据库，不复制本机业务数据。

## 2. 部署前准备

### 2.1 服务器要求

- Ubuntu 22.04 或 24.04
- 2 核 CPU、4 GB 内存、40 GB 以上磁盘
- Docker Engine 和 Docker Compose Plugin
- OpenSSL（Ubuntu 默认已安装）
- 公司内网 IP，或已通过 VPN、IP 白名单保护的服务器

系统已有单管理员登录，但测试端口仍不应直接暴露到公网。若必须公网访问，应在公司网关或云防火墙配置来源 IP 白名单，并启用 HTTPS。

### 2.2 上传项目

优先使用公司私有 Git 仓库。也可以从开发机执行：

```bash
rsync -av --delete \
  --exclude node_modules \
  --exclude .next \
  --exclude dist \
  --exclude .env \
  --exclude .env.staging \
  --exclude backups \
  "/Users/edy/Documents/对账系统/" \
  <服务器用户>@<服务器IP>:/opt/reconciliation-system/
```

部署目录示例：`/opt/reconciliation-system`。

## 3. 首次部署

### 3.1 创建测试环境配置

登录服务器后执行：

```bash
cd /opt/reconciliation-system
cp .env.staging.example .env.staging
chmod 600 .env.staging
```

生成一个数据库密码：

```bash
openssl rand -hex 24
```

编辑 `.env.staging`：

```dotenv
POSTGRES_DB=reconciliation_staging
POSTGRES_USER=reconciliation
POSTGRES_PASSWORD=<刚生成的密码>
TEST_HTTP_PORT=8080
WEB_URL=http://<服务器IP>:8080
```

数据库密码仅使用大小写字母和数字。`.env.staging` 不应上传到 Git 或发送到群聊。

### 3.2 创建管理员登录配置

```bash
sh scripts/generate-admin-credentials.sh admin
```

根据提示输入管理员密码，将输出的 `ADMIN_USERNAME`、`ADMIN_PASSWORD_HASH` 和 `SESSION_SECRET` 写入 `.env.staging`。密码哈希和会话密钥不要发送到群聊或提交到 Git。

HTTP 测试环境设置 `COOKIE_SECURE=false`；域名已经启用 HTTPS 时设置
`COOKIE_SECURE=true`。切换为 `true` 前必须确认浏览器实际通过 HTTPS 访问，否则
登录 Cookie 不会被浏览器保存。

### 3.3 启动系统

```bash
npm run staging:deploy
```

该命令会：

1. 检查环境变量。
2. 构建 API 与 Web 镜像。
3. 启动 PostgreSQL、API、Web 和 Nginx。
4. 自动执行数据库迁移。
5. 输出容器健康状态。

首次部署需要从 Docker Hub 和 npm 下载依赖，耗时取决于服务器网络。

## 4. 部署验收

### 4.1 检查容器

```bash
docker compose --env-file .env.staging -f compose.staging.yaml ps
```

4 个服务最终都应为 `Up` 或 `healthy`。

### 4.2 检查服务

```bash
curl http://127.0.0.1:8080/healthz
curl http://127.0.0.1:8080/api/health
```

健康检查是公开接口。返回内容中 API 和 database 均应为 `ok`；其他业务接口未登录时应返回 `401`。

浏览器打开：

```text
http://<服务器IP>:8080
```

使用管理员账号密码登录后，按以下顺序验收：

1. 进入“对账任务”。
2. 导入订单表、结算表、成本表和直播表。
3. 创建任务并开始对账。
4. 查看订单工作台、对账汇总、异常处理和成本版本。
5. 进入“千川消耗”导入千川消耗表并检查汇总。

## 5. 日常更新

更新代码前先备份数据库：

```bash
npm run staging:backup
```

将新代码上传到同一目录后执行：

```bash
npm run staging:deploy
```

Compose 会重新构建变化的镜像并滚动重启容器，数据库卷不会被删除。

不要使用以下命令，除非明确需要彻底删除测试数据：

```bash
docker compose --env-file .env.staging -f compose.staging.yaml down -v
```

其中 `-v` 会删除 PostgreSQL 数据卷。

## 6. 日志与排查

查看全部日志：

```bash
npm run staging:logs
```

只查看 API：

```bash
docker compose --env-file .env.staging -f compose.staging.yaml logs -f --tail=200 api
```

只查看数据库迁移或启动错误：

```bash
docker compose --env-file .env.staging -f compose.staging.yaml logs --tail=200 api postgres
```

重启单个服务：

```bash
docker compose --env-file .env.staging -f compose.staging.yaml restart api
```

## 7. 数据备份

执行：

```bash
npm run staging:backup
```

备份文件保存在 `backups/reconciliation-staging-日期时间.sql.gz`。数据库中包含导入的原始文件和对账结果，因此备份文件应按财务数据管理要求保存，不应发送到非受控位置。

测试期间建议每天备份一次，重要验收前再额外备份一次。

## 8. 环境隔离规则

- `reconciliation_staging`：供同事进行人工测试，数据长期保留。
- `reconciliation_test`：仅供开发机自动化端到端测试。
- 测试服务器不要执行 `npm run test:e2e`。
- 不要把本机 `.env`、测试环境 `.env.staging` 或数据库备份提交到 Git。
- 测试环境验证通过后，再单独设计生产环境的登录权限、HTTPS、审计和备份恢复方案。

## 9. 阿里云函数计算部署说明

公司测试环境使用阿里云函数计算时，不执行本文的 Docker Compose 命令。函数配置中
至少需要设置以下管理员环境变量：

```dotenv
ADMIN_USERNAME=<管理员账号>
ADMIN_PASSWORD_HASH=<脚本生成的 scrypt 哈希>
SESSION_SECRET=<脚本生成的会话密钥>
COOKIE_SECURE=true
```

会话保存在签名的 HttpOnly Cookie 中，不依赖函数实例内存，适合函数多实例运行。
登录失败次数限制当前保存在单个函数实例内，仅作为一期测试保护；后续如要求多实例
统一限流，需要另行设计共享限流存储。

实际发布前还需要由运维确认函数类型和 Node.js 版本、RDS PostgreSQL 与 VPC、
文件上传大小、函数超时时间、访问域名与 HTTPS，以及前后端路由方式。上述信息确认前，
不要直接按 Docker 手册发布到函数计算。

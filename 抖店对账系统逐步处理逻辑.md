# 抖店对账系统逐步处理逻辑

> 用途：说明抖店对账从数据导入到形成结果的完整过程。  
> 范围：抖店订单、结算、商品成本、直播列表和千川消耗。  
> 说明：已确认规则写入流程，未确定内容集中放在文档末尾。

## 一、整体流程

```text
导入并检查数据
→ 整理订单商品明细
→ 关联订单与结算
→ 匹配商品和成本
→ 判断订单类型和直播情况
→ 计算结算金额、成本和利润
→ 输出明细、汇总和问题清单
```

## 二、需要导入的数据

| 数据 | 一行代表什么 | 主要识别信息 | 用途 |
| --- | --- | --- | --- |
| 抖店订单表 | 一个子订单/商品 | 主订单编号、子订单编号 | 获取订单、商品、数量、达人和下单时间 |
| 抖店结算表 | 一笔平台结算记录，同一订单可能有多笔 | 订单号修复 | 获取平台实际结算结果 |
| 抖音商品成本表 | 一个商品的具体销售版本 | 商品id修复 + 商家编码 | 获取单位成本 |
| 直播列表 | 一场直播 | `live_id` | 判断订单是否发生在直播期间 |
| 千川01：小象汉字01 | 一个账号一天的消耗 | 账号 + 日期 | 查看广告消耗 |
| 千川02：百香果01 | 一个账号一天的消耗 | 账号 + 日期 | 查看广告消耗 |
| 千川03：朱颜 | 一个账号一天的消耗 | 账号 + 日期 | 查看广告消耗 |

售后退货表一期暂不导入。该表主要用于计算售后货损，后续需要时再接入。商品成本和利润先由订单表、结算表和商品成本表计算。APP 直播、自营出版物、日历等仪表盘属于统计结果，不需要单独导入。

## 三、处理步骤

### 第一步：导入并检查数据

1. 上传订单、结算、成本、直播和千川表；
2. 记录文件名称、导入时间和所属月份；
3. 检查必填字段、日期和数字格式；
4. 清理订单号、商品 ID 和商品编码中的空格；
5. 检查重复订单和重复结算，原始数据保留备查；
6. 重复导入同一批数据时，不能重复生成结果；
7. 导入文件中的全部原始字段和原始值都要保留，不能因为页面暂时不显示而删除；
8. 页面可以控制显示或隐藏哪些字段，被隐藏的字段仍保留在原始数据中，并可随时查看。

**输出：** 导入成功、重复、格式错误和缺少字段的数据数量及清单。

### 第二步：整理订单商品明细

当前订单表一行代表一个子订单/商品，已经包含主订单编号、子订单编号、商品 ID、商家编码和商品数量，不需要再拆分多个商品字段。

整理订单商品明细只是为了方便查看和计算，不会删除或覆盖原始订单字段。

同一主订单今后如果出现多个商品行，系统保留每个商品明细，并在主订单层面汇总。

**输出：** 可按子订单查看的商品明细，以及按主订单汇总的商品数量和金额。

### 第三步：关联订单与结算

```text
结算表“订单号修复” → 订单表“主订单编号”
```

1. 优先按订单号关联，不能按 Excel 行号或顺序关联；
2. 一个订单可以对应多条结算记录；
3. 正常结算、补充结算和退款记录全部保留；
4. 有订单无结算，标记“未结算”；
5. 有结算无订单，标记“缺订单或跨期结算”。
6. 下单时间和结算时间跨月时，另外标记“跨期结算”；即使订单未导入，结算表自带的下单时间也可用于判断。

结算表中的“结算金额”作为实际结算金额。退款记录为负数，直接参与合计。重复导入时，系统可按“子订单号 + 结算时间 + 结算单类型 + 结算金额”及整行内容检查重复，不需要业务人员手工指定去重字段。

```text
订单总结算金额
= 与该订单关联的全部平台结算记录金额之和
```

**输出：** 已关联订单、全部结算记录和关联异常清单。

### 第四步：匹配商品和成本

```text
订单表“商品 ID + 商家编码”
→ 商品成本表“商品id修复 + 商家编码”
```

同一个商品 ID 可能对应不同商家编码和不同成本，因此两个字段必须一起匹配。商品规格只用于人工核对，不作为自动匹配条件。商品 ID 或商家编码缺失、找不到唯一成本时，进入问题清单。

1. 为每项商品匹配商品名称和单位成本；
2. 商品成本直接按成本表匹配，不考虑商品分类；
3. 多商品订单分别匹配、计算后再汇总；
4. 没有匹配到有效成本，标记“缺成本”；
5. 同一匹配条件存在多个不同成本，标记“成本冲突”；
6. 缺成本和成本冲突不能按 `0` 元处理，处理后重新计算；
7. 每次导入成本表生成一个新版本并直接使用，不覆盖旧版本；
8. 已成功保存成本快照的历史订单不受新成本表影响；
9. 原来缺成本或成本冲突的订单，可以用新成本表重新匹配；
10. 正常结算的商品成本为正，退款对应的商品成本按负数记录。

```text
单项商品成本 = 单位成本 × 商品数量
订单总成本 = 所有商品均唯一匹配时，各商品成本之和
```

### 第五步：判断订单类型和直播情况

#### 1. 自营或达人

```text
达人 ID 为空、为 0 或为 108314295234 → 自营订单
其他达人 ID → 达人订单
```

#### 2. 是否在直播期间

```text
直播开始时间 ≤ 订单提交时间 ≤ 直播结束时间
```

- 自营订单匹配到直播场次：自营上播；
- 自营订单未匹配到直播场次：自营非上播；
- 达人订单不参与自营上播判断。

匹配成功后，将 `live_id` 记录为直播场次编号。`live_id` 不是订单关联字段，实际通过时间范围匹配。

系统根据“抖音号 + 直播开始时间”生成稳定的内部场次编号，例如 `DY-xiaoxianghanzi-20260722110021`。同一订单如果同时落入多场直播，标记“直播冲突”并进入问题清单；订单提交时间为空时标记“缺少下单时间”。

### 第六步：计算金额

平台结算金额直接使用结算表中的平台结果，不用订单支付金额代替。退款、服务费和达人佣金等如果已包含在平台结果中，不能再次计算。

```text
订单总结算金额 = 该订单所有平台结算记录金额之和

订单总成本 = 每项商品的单位成本 × 商品数量后相加（全部商品成本匹配成功时）

订单贡献利润 = 订单总结算金额 - 订单总成本
```

只有订单已有结算且全部商品成本都匹配成功时才计算利润。退款对应的商品成本按负数记录。千川消耗一期单独展示，不进入订单贡献利润。

财务对账默认按结算月份汇总，同时保留订单月份用于经营分析和跨期查询。

### 第七步：输出对账结果

#### 1. 对账明细

每个订单展示：订单编号和时间、商品、自营或达人、直播场次、全部结算记录、总结算金额、商品成本、利润、对账状态和问题原因。

#### 2. 对账汇总

可按订单月份、结算月份、自营或达人、是否上播、直播场次和达人汇总数量及金额。

#### 3. 问题清单

| 问题 | 处理方式 |
| --- | --- |
| 有订单无结算 | 标记“未结算”，不计入已结算收入 |
| 有结算无订单 | 标记“缺订单或跨期结算”，保留结算金额 |
| 一个订单多条结算 | 全部保留并合计 |
| 订单商品字段缺失 | 标记“订单商品信息不完整”，人工确认 |
| 商品无法匹配 | 标记“缺商品信息” |
| 商品没有成本 | 标记“缺成本”，不能按 0 元计算 |
| 同一商品与商家编码对应多个成本 | 标记“成本冲突”，解决前不计算利润 |
| 同时匹配多场直播 | 标记“直播冲突”，不自动指定场次 |
| 订单缺少提交时间 | 标记“缺少下单时间”，不判断上播 |
| 运费、保证金、平台单独扣款 | 保留并与正常商品结算分开显示 |

#### 4. 千川消耗

千川按“账号 + 日期”展示余额总消耗、非赠款消耗、赠款消耗、消返红包、立减红包、共享钱包和共享赠款消耗。一期不分摊到订单、商品或直播场次。

## 四、最终结果

1. **对账明细：** 每个订单结算多少、成本多少、利润多少；
2. **对账汇总：** 不同月份、商品、订单类型和直播场次的数量及金额；
3. **问题清单：** 没有关联、没有成本或需要人工确认的数据。

所有汇总结果都应能查看对应订单、结算记录和来源文件。

## 五、已确认和待核对内容

1. 成本按“商品 ID + 商家编码”匹配，并保存订单商品成本快照。
2. 每次导入成本表生成独立版本，导入后直接使用，不需要确认生效日期。
3. 新成本表只处理尚未匹配成本、缺成本或成本冲突的订单商品，不覆盖已成功匹配的成本快照。
4. 历史订单继续使用第一次匹配到的旧成本；新订单使用新导入的成本表。
5. 有结算且全部商品成本匹配成功时计算利润；缺成本或成本冲突时不计算。

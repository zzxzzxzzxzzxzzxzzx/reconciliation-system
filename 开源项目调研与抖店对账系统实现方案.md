# 开源项目调研与抖店对账系统实现方案

> 状态：方案已确认，本文只做方案，不包含业务代码。  
> 一期范围：抖店订单、抖店结算、商品成本、直播场次；千川消耗独立导入和展示。小红书暂不接入。

## 1. 调研结论

GitHub 上没有一个可以直接覆盖“抖店订单 + 结算 + 成本 + 直播 + 千川”的完整项目。可复用的是几类通用做法：

| 项目 | 借鉴点 | 对本项目的启发 |
| --- | --- | --- |
| [tiktok-shop-settlement](https://github.com/Perufitlife/tiktok-shop-settlement) | 结算文件解析、统一字段名、兼容平台导出格式 | 先把 Excel/CSV 转成统一字段，再进入对账 |
| [reconify](https://github.com/reconifyhq/reconify) | 确定性的匹配规则、可配置字段映射、异常结果可追溯 | 订单号优先匹配，匹配失败必须保留原因 |
| [payment-reconciliation-hub](https://github.com/jdimodugno/payment-reconciliation-hub) | Provider 适配层、幂等导入、异步任务和重试 | 抖店、千川使用相同导入框架，平台差异放在适配器内 |
| [transaction-reconciliation](https://github.com/mrioan/transaction-reconciliation) | 对账核心与页面查询分离 | 计算逻辑独立成服务，页面只查询结果 |
| [OCA/bank-statement-import](https://github.com/OCA/bank-statement-import) | 可插拔的多格式导入扩展 | 每种表格一个解析器，不在业务层堆文件格式判断 |
| [ttspc-server-sample](https://github.com/tiktok/ttspc-server-sample) | 平台授权和 API 集成样例 | 一期先文件导入，后续接平台 API 时沿用适配器边界 |

### 1.1 主要踩坑

1. 同一订单可能多次结算、补结算或退款，不能把订单和结算设计成一对一。
2. Excel 订单号容易出现空格、科学计数法和前导零丢失，必须按字符串清洗和保存原值。
3. 重复上传同一文件会造成重复金额，导入必须有批次和幂等键。
4. 商品成本会变化，历史订单不能跟着当前成本变化，必须保存成本生效区间和订单计算时使用的成本快照。
5. 关联失败不能静默忽略，也不能用 0 元代替缺失成本，否则汇总会失真。
6. 直播场次可能时间重叠，不能凭页面顺序猜归属，需要明确冲突状态。
7. 千川消耗与订单没有可靠的一对一关系，一期只按账号和日期展示，不强行分摊。

## 2. 推荐技术栈

在没有既有技术栈约束的前提下，采用 TypeScript 全栈：

| 层次 | 选择 | 原因 |
| --- | --- | --- |
| 前端 | Next.js + React + Ant Design | 适合后台表格、筛选、详情和导入反馈 |
| 后端 | NestJS + TypeScript | 模块边界清晰，适合导入、对账、异常等领域模块 |
| 数据库 | PostgreSQL | 事务、唯一约束、时间区间查询和聚合能力足够 |
| ORM/迁移 | Prisma | 类型安全，迁移和本地开发简单 |
| 任务执行 | NestJS 同步任务 | 一期数据量可控，减少部署依赖；超出函数时限后再评估异步队列 |
| 文件解析 | `xlsx` + `csv-parse` | 覆盖当前 Excel/CSV 文件来源 |
| 测试 | Vitest/Jest + Supertest | 覆盖计算规则和接口 |
| 部署 | Docker Compose（一期） | 本地、测试环境启动方式一致 |

如果团队已有 Java 体系，可将后端替换为 Spring Boot，数据库、领域模型、导入批次和对账流程保持不变；在没有约束时优先采用上面的 TypeScript 方案，减少前后端语言切换。

## 3. 总体架构

一期采用“模块化单体”，不拆微服务：

```text
Next.js 管理后台
        ↓ HTTP/JSON
NestJS API
  ├─ 导入模块（批次、解析、校验、幂等）
  ├─ 标准数据模块（订单、商品明细、结算、直播、千川）
  ├─ 对账模块（订单号关联、成本匹配、金额计算）
  ├─ 成本模块（历史版本、快照、重算）
  └─ 异常与追溯模块
        ↓
PostgreSQL（业务数据、任务状态与结果）
```

核心原则：原始数据不可变、标准数据可查询、计算结果可重算、每个结果可追溯到导入批次和原始行。

## 4. 核心数据模型

### 4.1 导入与原始数据

| 表 | 一行代表 | 主键/幂等键 |
| --- | --- | --- |
| `import_batch` | 一次文件导入 | `id`；文件哈希 + 数据类型 + 账期 |
| `raw_record` | 原始文件的一行 | `id`；批次 ID + 行号 |
| `import_error` | 一条导入错误 | `id` |

`raw_record` 保存原始字段 JSON、原始文件名、原始行号和批次 ID，后续标准化失败也不丢数据。

### 4.2 业务数据

| 表 | 一行代表 | 关键字段/关系 |
| --- | --- | --- |
| `douyin_order` | 一个主订单 | `order_no` 唯一；关联多个商品明细和结算记录 |
| `douyin_order_item` | 主订单中的一个商品明细 | 订单 ID、商品 ID、商家编码、数量 |
| `douyin_settlement` | 一笔平台结算记录 | 订单号、结算时间、金额、结算类型；允许一单多条 |
| `live_session` | 一场直播 | `live_id`、开始时间、结束时间 |
| `qianchuan_spend` | 一个账号某日的一条消耗记录 | 账号、日期、各类消耗金额 |
| `reconciliation_result` | 一个订单的一次计算结果 | 订单 ID、结算合计、成本合计、利润、状态 |
| `reconciliation_issue` | 一条可处理的问题 | 订单/记录 ID、问题类型、原因、处理状态 |

### 4.3 历史成本快照

商品成本不覆盖更新，采用“版本表 + 订单快照”：

| 表 | 作用 |
| --- | --- |
| `product_cost_version` | 保存商品 ID、商家编码、单位成本、导入顺序、版本号和来源批次 |
| `order_item_cost_snapshot` | 订单计算时复制实际使用的成本、成本版本 ID 和匹配依据 |

匹配规则：

```text
订单商品 ID + 商家编码 = 本次成本版本的商品 ID + 商家编码
```

订单第一次成功匹配成本时保存快照。以后导入同商品的新成本版本不会改写历史订单；未匹配、缺成本或成本冲突的订单可以使用新版本重新匹配。

## 5. 业务处理链路

### 5.1 导入和标准化

输入文件 → 识别数据类型 → 表头映射 → 字段清洗 → 必填校验 → 幂等检查 → 写入标准表。

输出：导入批次状态、成功/重复/失败数量、错误行和原始数据链接。

### 5.2 订单与结算关联

`结算表.订单号修复 → 订单表.主订单编号`。先清洗空格和数字格式，再按订单号关联；允许一对多；有订单无结算、有结算无订单分别进入问题清单。

### 5.3 商品成本匹配

按“商品 ID + 商家编码”匹配本次导入的成本版本。已成功保存成本快照的订单保持不变；找不到商品或成本时保留问题状态，不按 0 元计算。

### 5.4 计算规则

```text
订单总结算金额 = 关联结算金额之和
订单总成本 = 各商品数量 × 对应历史成本快照之和
订单贡献利润 = 订单总结算金额 - 订单总成本
```

退款结算金额和对应商品成本按负数进入合计。千川消耗不进入订单利润。

## 6. 模块划分

### 后端

- `imports`：批次、文件、表头映射、校验、幂等、异步任务。
- `orders`：订单主表、商品明细、原始记录查询。
- `settlements`：结算记录、多笔结算、结算月份和金额。
- `products`：商品标识和成本版本维护。
- `reconciliation`：关联、成本快照、计算版本、重算。
- `live-sessions`：直播场次和时间匹配；时间重叠标记冲突。
- `qianchuan`：账号日期消耗导入、查询和汇总。
- `issues`：异常清单、人工处理状态和处理记录。

### 前端

- 数据导入：上传、字段预览、校验结果、批次进度。
- 对账结果：筛选、汇总、异常状态和导出。
- 订单详情：订单、商品、结算、成本快照、利润、原始行追溯。
- 成本维护：按商品查看各次导入版本；新版本直接使用，但禁止覆盖历史版本和已成功匹配的订单快照。
- 问题清单：按问题类型筛选，查看原始数据和处理状态。
- 千川消耗：按账号和日期查看，不显示订单分摊入口。

## 7. 分阶段开发顺序

### 阶段 0：项目骨架和验证

建立 Next.js、NestJS、PostgreSQL、Prisma 和 Docker Compose；补充健康检查、环境变量模板和基础测试命令。

验收：新环境可以一条命令启动，前后端健康检查通过。

### 阶段 1：导入底座

完成 `import_batch`、`raw_record`、`import_error`，实现 CSV/Excel 解析、表头映射、清洗、必填校验、文件哈希幂等和导入结果查询。

验收：同一文件重复上传不会重复入库，错误能定位到文件和行号。

### 阶段 2：抖店订单和结算

实现订单主表、商品明细、结算表及导入；按订单号建立一对多关联；先提供接口和自动化测试，不急着做复杂页面。

验收：真实样例能查到一个订单的全部商品和全部结算记录。

### 阶段 3：历史成本与对账计算

实现成本版本、订单成本快照、订单总结算金额、订单成本和利润计算；支持新成本表修复缺成本或成本冲突。

验收：成本更新后，旧订单仍使用原快照，新订单及未解决成本问题使用新版本；版本来源可追溯。

### 阶段 4：直播和异常处理

实现直播场次导入、订单时间匹配、重叠场次冲突标记，以及缺订单、未结算、缺商品、缺成本等问题清单。

验收：每个异常均可定位到原始批次和行号，冲突场次不自动猜测。

### 阶段 5：查询页面和汇总

实现导入页、对账明细、订单详情、问题清单、成本版本维护、汇总页。

验收：从汇总可以下钻到订单，再下钻到结算记录、成本快照和原始行。

### 阶段 6：千川独立模块

实现千川三类账号的导入、幂等、按账号/日期查询和汇总；不与订单利润耦合。

验收：千川消耗可独立核对，重复导入不重复统计。

## 8. 关键风险与防护

| 风险 | 防护 |
| --- | --- |
| 订单号被 Excel 改格式 | 全部按字符串读取，同时保存原始值和清洗值 |
| 重复导入造成重复金额 | 文件哈希、业务幂等键和数据库唯一约束三层控制 |
| 成本更新影响历史利润 | 独立成本版本 + 首次成功匹配的订单成本快照 |
| 多笔结算漏算 | 结算表与订单一对多，金额按全部记录合计 |
| 关联失败被忽略 | 每条失败记录生成问题，不直接丢弃 |
| 直播时间重叠 | 标记 `CONFLICT`，人工确认后再归属 |
| 大文件导入阻塞请求 | 一期限制作业文件规模并设置合理超时；规模扩大后再引入异步队列 |
| 计算规则被页面改写 | 规则只在后端领域服务中实现，页面不重复计算 |

## 9. 已确认的开发依据

1. 技术栈采用 `Next.js + NestJS + PostgreSQL + Prisma`，一期不引入 Redis 或消息队列。
2. 商品成本按“商品 ID + 商家编码”匹配当前导入版本，并保存订单成本快照；新版本不覆盖已成功匹配的历史订单。
3. 真实数据样例以飞书“小象汉字APP账单结算”中的抖音订单表、结算表、商品成本表和直播列表为准。
4. 已确认抖音目录包含订单表、结算表、对账原则、直播列表、三张千川表和售后退货表；一期仍不接入售后退货表。
5. 开发导入模块时，需要基于飞书真实表逐项建立表头映射；字段原值全部保留，业务计算只使用确认后的标准字段。

成本表使用“商品id修复 + 从商品id 和 编码集合中提取的商家编码”匹配订单。每次导入生成独立版本，不要求成本表提供生效时间。

分润、售后货损、千川订单分摊和小红书接入不影响一期技术骨架，暂不进入本次开发。

## 10. Cursor 执行方式

确认方案后，按阶段逐条执行，不要求 Cursor 一次生成全部系统。每次执行前让 Cursor 先输出准备修改的文件和数据变更，完成后运行测试并汇报结果；禁止擅自修改需求文档和执行 `git commit`。

# 对账系统使用手册

版本范围：一期抖店订单、结算、商品成本、直播场次，以及千川消耗。

## 1. 系统能做什么

系统以订单为中心，把四类抖店数据放到同一次对账中：

- 订单表：订单、商品、下单时间、达人信息。
- 结算表：平台实际结算和退款记录。
- 商品成本表：商品单位成本及成本版本。
- 直播明细表：直播场次和直播时间。

系统会输出订单明细、对账汇总和异常清单。千川消耗独立展示，当前不分摊到订单、商品或直播场次，也不计入订单利润。

### 当前真实数据验收结果

以当前抖店 2026 年 7 月订单、结算、成本和直播文件验收：327 个订单、342 条商品明细、138 个已结算订单，订单总结算金额为 ¥14,242.00；231 条商品明细已匹配成本，90 条缺成本，21 条成本冲突；95 个订单可计算利润，合计 ¥4,770.36。

当前千川文件已导入“小象汉字01”账号 21 条日消耗记录，合计 ¥2,875.82。

## 2. 启动系统

### 2.1 环境要求

- Node.js 22 LTS
- npm 10 或更高版本
- Docker Desktop

### 2.2 第一次启动

在项目根目录执行：

```bash
nvm use
cp .env.example .env
sh scripts/generate-admin-credentials.sh admin
npm install
npm run infra:up
npm run db:generate
npm run db:migrate
npm run dev
```

密码生成脚本会要求输入至少 12 个字符的密码。将脚本输出的
`ADMIN_USERNAME`、`ADMIN_PASSWORD_HASH` 和 `SESSION_SECRET` 写入 `.env`。
本地 HTTP 环境使用 `COOKIE_SECURE=false`，部署到 HTTPS 环境时改为
`COOKIE_SECURE=true`。

打开：

- 前端：http://localhost:3100
- 后端健康检查：http://localhost:3001/health

健康检查返回 `status: ok` 且 database 为 `ok`，才可以开始操作。

### 2.3 管理员登录

打开前端后先使用管理员账号和密码登录。系统当前只设一个管理员账号，不提供
注册、多账号、角色权限或找回密码。未登录时不能进入业务页面，也不能调用业务
接口；退出后会立即返回登录页。

本机开发环境当前临时账号为 `admin`，临时密码为 `Admin123456!`。部署测试环境
前必须使用密码生成脚本更换账号密码和会话密钥，不要把明文密码写入代码或发到
群聊。

### 2.4 停止和验证

```bash
npm run infra:down
npm run lint
npm run build
npm test
npm run test:e2e
```

`infra:down` 只停止本地 Docker 服务，不删除代码；数据库删除操作不要直接执行。

## 3. 开始前准备文件

建议先把同一对账月份的文件放在一个本地目录，并确认文件名和月份。每种文件只选择对应平台、对应月份的数据。

### 3.1 订单表

必填字段：`主订单编号`。

常用字段：子订单编号、商品 ID、商家编码、选购商品、商品数量、商品金额、订单提交时间、达人 ID、订单状态。

一行通常代表一个订单或一个商品明细。多商品字段如果无法一一对应，系统会保留原始值并生成“商品拆分待确认”。

### 3.2 结算表

必填字段：`订单号`。

常用字段：结算时间、子订单号、结算金额、结算单类型、下单时间、收入合计、支出合计、平台服务费、达人佣金。

文件中的说明行会被跳过。退款金额为负数，会直接参与订单结算金额合计。

### 3.3 商品成本表

必填字段：`商品id修复`。

用于匹配的字段：`商品id修复`、`商品id 和 编码集合`、`成本`。系统会从“商品id 和 编码集合”中拆出商家编码。

成本表里同一“商品 ID + 商家编码”有多个不同成本时，会产生“成本冲突”。

### 3.4 直播明细表

必填字段：`直播开始时间`。

常用字段：主播抖音号、主播昵称、直播开始时间、直播结束时间、直播时长（分钟）。

一行代表一场直播。开始时间和结束时间必须有效，且结束时间不能早于开始时间。

### 3.5 千川消耗表

通过“千川消耗”页面单独导入，并选择账号：小象汉字01、百香果01或朱颜。

需要包含日期、余额总消耗及至少一个消耗分类字段。包含“总计”汇总行时，系统不会把它重复计入日明细。

## 4. 推荐操作流程

推荐使用“对账任务”页面完成一整次对账。不要先在多个页面随意选择不同月份的批次。

### 第一步：进入“对账任务”

1. 点击顶部“对账任务”。
2. 选择“结算月份（对账归属月）”。例如，按 2026 年 7 月结算数据对账，就选择 `2026-07`。
3. 分别选择订单表和结算表，这两项必选。
4. 成本表、直播表可以同时选择；如果后续再补充文件，需要使用新的文件批次新建一条对账任务，原任务和原结果继续保留。
5. 确认四个批次的文件名、成功行数和导入时间无误。
6. 点击“创建对账任务”。

如果订单文件中的订单号已经在其他批次处理过：全部重复时，系统会自动改用原订单批次并提示“检测到订单号全部已存在，已自动使用原订单批次”；只有部分重复时，系统会阻止创建任务。此时应选择原订单批次，或重新导出只包含新增订单的文件。

如果对账月份以及订单、结算、成本、直播批次都相同，系统不会重复创建任务，而是提示“相同对账任务已存在”。原任务已归档时会自动恢复。

也可以直接在对应数据框右侧点击“导入”，上传后系统会自动选中新批次。

### 第二步：开始对账

1. 在“任务记录”找到刚创建的月份和文件组合。
2. 点击“开始对账”。
3. 系统依次处理订单、结算、成本和直播，并保存本次任务使用的数据范围。
4. 运行成功后状态变为“已完成”；失败时查看任务记录中的错误说明。

同一任务不能并发启动。运行中不要重复点击“开始对账”。

任务记录支持“归档”和“删除”：待运行、失败任务可以删除；重复的已完成任务可以逐条删除，但系统会强制保留一条；处理中任务和唯一的已完成任务不能删除。删除任务只删除任务记录，不会删除订单、结算、成本、直播文件或已经生成的业务数据。

### 第三步：查看结果

已完成任务右侧点击“查看结果”。结果页面会使用该任务绑定的订单批次和结算批次，不会把其他结算批次混入本次结果。多个任务需要合并查看时，使用“对账汇总”按月份查看。

在“订单工作台”可以：

- 搜索订单号；
- 按自营、达人筛选；
- 按直播状态筛选；
- 按问题类型筛选；
- 调整每页条数并翻页；
- 点击订单号或“查看”打开订单详情。

订单详情的“结算记录”右侧可以点击“补充结算表”；已有结算记录时按钮显示为“补充后续结算表”。系统会进入对账任务并定位结算表，不允许在订单详情中手工填写结算金额。

订单超过 500 条时，页面会自动分批读取，仍可正常分页查看。

## 5. 四张表如何关联

### 5.1 订单和结算

```text
订单表：主订单编号
结算表：订单号修复
```

一笔订单可以对应多条结算记录。订单总结算金额为全部关联结算记录金额之和，包括退款、补充结算等记录。

### 5.2 订单和成本

```text
订单表：商品 ID + 商家编码
成本表：商品id修复 + 从商品id和编码集合拆出的商家编码
```

商品规格用于人工核对，不参与自动匹配。缺任一关键字段、找不到成本或同一匹配键出现多个不同成本时，订单不能计算利润。

### 5.3 订单和直播

自营判断规则：达人 ID 为空、`0` 或 `108314295234` 视为自营；其他值视为达人订单。

自营订单按以下时间条件匹配直播：

```text
直播开始时间 ≤ 订单提交时间 ≤ 直播结束时间
```

匹配一场为“自营上播”，没有匹配为“自营非上播”，同时落入多场为“直播冲突”，缺少订单提交时间为“缺少下单时间”。达人订单显示“达人订单”，不参与自营直播场次判断。

## 6. 金额和利润怎么看

```text
订单总结算金额 = 全部关联平台结算记录金额之和
单项商品成本 = 单位成本 × 商品数量
订单总成本 = 全部商品明细成本之和
订单贡献利润 = 订单总结算金额 - 订单总成本
```

只有同时满足以下条件，利润才会显示：

1. 订单至少有一条结算记录；
2. 订单所有商品都有唯一有效成本；
3. 商品数量和成本快照可用于计算。

缺成本、成本冲突或未结算时，系统显示“暂不可计算”，不会用 0 元代替。

### 6.1 历史成本

每次导入成本表都会形成独立成本版本，不覆盖旧版本。订单第一次成功匹配后会保存成本快照；后续导入新成本表不会改写已经成功匹配的历史订单。

新成本表只会重新处理尚未匹配、缺成本或成本冲突的商品。

### 6.2 人工新增和更正成本

进入顶部“成本版本”，先选择一份完整成本版本作为基础，再选择维护方式：

- 新增成本：用于补充成本表遗漏的商品，填写商品 ID、商家编码、单位成本和补充原因。
- 更正成本：用于商品成本发生变化，先在版本商品明细中找到商品并点击“更正”，填写新单位成本、开始适用日期和更正原因。

保存后系统会复制基础版本并生成一份完整的新版本，不直接修改原版本。新增或更正的成本批次会出现在“对账任务”的成本表选择框中；需要选择该批次并新建、运行对账任务，才能修复缺成本订单或计算后续订单。

更正成本按订单发生时间判断：开始适用日期之前的订单使用旧成本，开始适用日期当天及之后的订单使用新成本。已经成功保存成本快照的历史订单不会被覆盖。

“商品 ID + 商家编码”已存在时不能重复新增，应使用“更正成本”；基础版本中不存在的商品不能使用更正，应使用新增。人工版本可以下载原始文件，保留维护原因和来源记录。

## 7. 对账汇总

进入“对账汇总”后，选择“对账月份”。系统会合并该月份所有未归档且已完成的对账任务，并按订单号去重后统计。

页面展示订单数、结算金额、成本、可计算利润、结算进度、成本状态和问题分布。订单下单月份不作为汇总条件；跨期结算按结算月份归属。

点击问题类型可以进入异常处理页面。来源文件和来源批次会显示在汇总区域，便于追溯。

汇总只计算所选月份内未归档且已完成任务对应的数据。同一订单号只统计一次，同一结算批次不会重复累计；补充任务会合并到同月结果。来源文件和来源批次会显示在汇总区域，便于追溯。历史任务留下的问题不会混入当前对账结果；“商品无法匹配”是“缺成本”的匹配原因，不会在汇总问题数中重复计算。

## 8. 异常处理

进入“异常处理”，先选择订单批次，再按问题类型筛选。常见问题如下：

| 状态 | 含义 | 处理方式 |
| --- | --- | --- |
| 未结算 | 订单没有找到本次结算记录 | 确认是否尚未结算或结算跨月 |
| 缺订单 | 结算记录没有对应订单 | 补充正确订单表，或确认是否为跨期数据 |
| 跨期结算 | 订单月份和结算月份不同 | 按财务结算月份归属，同时保留订单月份 |
| 商品 ID 缺失 | 订单商品没有商品 ID | 补充或修正订单数据 |
| 商品无法匹配 | 系统记录的缺成本匹配原因 | 在异常页以“缺成本”统一处理，修正成本表或订单字段后重新匹配 |
| 缺成本 | 没有有效成本，利润暂不计算 | 补充成本表后重新匹配 |
| 成本冲突 | 同一匹配键有多个不同成本 | 修正成本表，保证同一版本只有一个有效成本 |
| 直播冲突 | 订单同时落入多场直播 | 核对直播时间，确认是否需要调整直播数据 |
| 缺少下单时间 | 无法判断直播归属 | 补充订单提交时间 |

打开异常详情后可以查看问题说明、匹配详情、来源文件、来源行号和原始数据。填写处理状态和说明后，点击“保存处理结果”。在问题清单上按异常类型、处理状态或关键词筛选后，点击“导出异常清单”，下载的文件只包含当前筛选结果，并带有异常原因、来源批次、原始行号和原始数据；导出内容使用中文异常名称和处理状态，便于直接分派处理。

详情会根据问题提供对应入口：缺成本、成本冲突可补充成本表；未结算可补充结算表；缺订单、商品 ID 缺失、缺少下单时间和商品拆分待确认可补充订单表；直播冲突可补充直播表。入口只会跳转到“对账任务”并突出对应数据框，不会直接改写原始数据。补充文件后，需要确认批次组合并创建新的对账任务；旧任务和旧结果会继续保留用于追溯。

从异常详情创建的新任务会显示“异常补充”来源。任务运行完成后点击“查看新结果”；如果异常仍然存在，异常详情会显示已创建的补充任务及其状态，并可返回任务或查看新结果。创建和运行补充任务不会自动把旧异常标记为已解决。

跨期结算表示订单下单月份与平台结算月份不同。例如订单在 2026 年 6 月下单、平台在 2026 年 7 月结算，订单仍保留为 6 月订单，结算金额计入 7 月对账。确认这是正常跨期后，可以点击“确认按结算月归属”；系统只保存“已确认”和处理说明，不修改下单时间、订单金额、结算金额或原始数据。

## 9. 批次生命周期

主界面已移除独立的“数据管理”模块，日常文件上传、批次选择和成本补充统一在“对账任务”中完成。

系统仍会在后台保留每个导入批次，用于任务关联、来源追溯和历史成本快照。已经参与任务或生成业务数据的批次不会被删除；误导入且未使用的批次才允许由后台维护人员清理。

这项调整只移除了前端入口，不删除已有批次、原始文件、订单、结算、成本或直播数据。

## 10. 千川消耗

1. 点击顶部“千川消耗”。
2. 选择账号。
3. 选择 CSV 或 XLSX 文件并上传。
4. 如果文件不是千川消耗表，页面会提示“文件格式不符合千川消耗表要求”。
5. 按账号、日期范围筛选数据。
6. 查看总消耗、非赠款、赠款、红包、共享钱包等指标。
7. “导出查询结果”只导出当前筛选结果，不是原始文件下载。

同一账号、同一日期在同一批次内只能保留一条日消耗记录；重复导入同一文件不会重复增加数据。

## 11. 原始数据追溯

系统会保留：

- 来源批次 ID；
- 原始文件名；
- 原始行号；
- 导入时的原始字段和值；
- 成本匹配使用的成本版本和成本行号。

在订单详情中可以分别核对订单来源、结算来源和成本来源。在异常详情中可以打开对应订单，并查看匹配详情和原始数据。

## 12. 常见问题

### 页面没有订单

确认：

1. 是否已上传订单表；
2. 订单批次是否处理成功；
3. 当前是否选中了正确的订单批次；
4. 是否打开了搜索或问题筛选导致结果为空。

### 创建任务失败，提示订单号已在其他批次处理过

这表示当前订单文件与已经处理过的订单有重复。全部重复的文件系统会自动复用原订单批次；部分重复时不会只导入其中的新订单，以免遗漏或重复计算。请选择原订单批次，或上传只包含新增订单的文件。

### 成本表导入后没有匹配

确认订单和成本表的 `商品 ID`、`商家编码` 是否都存在且清理了空格、单引号。导入新的成本表后，需要选中新成本批次并创建、运行一条新的对账任务；单纯上传文件不会改变订单成本快照，原任务和已成功匹配的历史成本不会被覆盖。

### 同一个商品为什么有多个成本

只有同一个成本版本内、同一个“商品 ID + 商家编码”对应多个不同成本，才会产生“成本冲突”。不同成本版本之间的价格变化属于历史版本，不自动视为冲突。

### 人工新增或更正成本后为什么订单没有变化

人工维护只会生成新的成本版本，不会直接改写订单。进入“对账任务”，选择新生成的人工成本批次，与对应订单、结算批次创建并运行一条新任务。已成功匹配成本的历史订单仍保留原成本快照。

### 任务已完成但结果金额不对

先检查“查看结果”打开的任务月份和结算文件，再检查是否把同一订单的退款、补充结算或跨期结算纳入了本次数据。汇总页按月份合并任务，已归档任务不会参与统计。

### 文件名显示乱码

当前版本会保留正常中文文件名，并只对检测到乱码特征的文件名尝试恢复。刷新页面即可查看修复后的名称；原始文件内容不受文件名显示修复影响。

### 保存异常处理没有变化

确认已经选择处理状态。处理说明可以为空，但状态必须是“待处理、待补订单数据、已确认或已解决”之一。保存成功后刷新异常列表验证。

## 13. 当前范围和暂不支持内容

- 当前只实现抖店和千川模块；小红书、其他平台尚未接入。
- 售后退货表一期暂不导入，不参与当前利润计算。
- 千川消耗一期只按账号和日期展示，不分摊订单、商品或直播。
- 当前未接入权限和多用户审批；适合本地或受控内网环境使用。

## 14. 开发验证

修改代码后，在项目根目录执行：

```bash
npm run lint
npm run build
npm test
npm run test:e2e
```

端到端测试会使用独立的测试数据库，不会清理或修改本地业务数据库。

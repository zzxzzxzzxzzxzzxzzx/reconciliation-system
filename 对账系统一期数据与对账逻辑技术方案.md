# 抖店对账需求分析（讨论版）

> 用途：开会确认业务需求，不讨论具体技术实现。  
> 依据：2026 年 8 月 6 日、8 月 11 日会议，以及“小象汉字 APP 账单结算”中的抖音表格。  
> 范围：一期梳理抖店对账；千川独立展示，小红书另行梳理。

## 1. 一期目标

1. 导入订单、结算、直播和商品成本数据；
2. 按订单号关联订单与结算；
3. 区分自营、达人和直播期间；商品分类暂不纳入一期计算；
4. 计算平台结算金额、商品成本和订单贡献利润；
5. 单独处理缺订单、未结算、缺成本和无法匹配的数据；
6. 千川按账号和日期展示消耗，不分摊到订单；
7. 所有结果可以追溯到原始记录。

## 2. 数据来源

| 数据 | 一行代表什么 | 主要识别信息 | 用途 |
| --- | --- | --- | --- |
| 订单表 | 一个主订单，可能包含多个商品信息 | 主订单编号、商品信息、商家编码、数量、金额、订单提交时间 | 订单主数据 |
| 结算表 | 一笔结算记录，同一订单可能有多条 | 订单号修复、结算时间、平台结算结果、订单月份 | 财务结算依据 |
| 抖音商品成本表 | 一个商品的具体销售版本 | 标准商品 ID + 商品编码 | 单位成本 |
| 直播列表 | 一场直播 | 主播抖音号、开始时间、结束时间 | 判断订单是否在直播期间 |
| 千川01：小象汉字01 | 一个账号一天的消耗 | 账号 + 日期 | 广告消耗展示 |
| 千川02：百香果01 | 一个账号一天的消耗 | 账号 + 日期 | 广告消耗展示 |
| 千川03：朱颜 | 一个账号一天的消耗 | 账号 + 日期 | 广告消耗展示 |
| 售后退货表 | 一张退换单 | 退换单号 | 一期暂不参与对账 |

订单表当前确认是一行一个主订单，多个商品信息和商家编码可能合并在一行。具体分隔符、子订单号和规格字段仍需确认。

千川实际字段包括：日期、余额总消耗、非赠款消耗、赠款消耗、消返红包、立减红包、共享钱包和共享赠款消耗。

以下页面是统计结果，不作为独立数据导入：

- APP 直播、自营出版物、日历三个对账仪表盘；
- 直播与非直播月份统计；
- 结算节点总看板。

## 3. 核心流程

```text
导入数据
  ↓
检查格式和重复数据
  ↓
清理订单号、商品 ID 和商品编码
  ↓
按订单号关联订单与结算
  ↓
拆分多商品信息，匹配成本和直播场次
  ↓
判断订单类型，计算金额
  ↓
输出对账结果和问题清单
```

## 4. 订单与结算

### 4.1 关联规则

```text
结算表“订单号修复” → 订单表“主订单编号”
```

1. 关联前先清理订单号中的空格和数字格式；
2. 一个订单可以对应多条结算记录，所有记录都要保留；
3. 不能按照 Excel 行号或表格顺序关联；
4. 子订单号只能在确认实际字段存在后作为辅助核对信息；
5. 订单表先按主订单编号检查重复；结算表的重复判断字段仍需确认。

### 4.2 多商品订单

同一主订单中的商品信息、商家编码和数量需要按确认后的分隔符拆开，并保持相同位置对应。数量不一致时不能自动猜测，进入问题清单。

### 4.3 异常处理

| 情况 | 处理方式 |
| --- | --- |
| 有订单、没有结算 | 标记“未结算”，不计入已结算收入 |
| 有结算、没有订单 | 标记“缺订单或跨期结算”，保留结算金额 |
| 一个订单有多条结算 | 全部保留，在订单详情中合计展示 |
| 多商品信息无法对应 | 标记“订单拆分失败”，人工确认 |
| 商品或成本无法匹配 | 标记“缺商品信息”或“缺成本”，不能按 0 元计算 |

运费、保证金和平台单独扣款等特殊记录需要保留，并与正常商品结算分开显示。

## 5. 订单分类

订单需要分层判断：

### 5.1 自营或达人

- 达人 ID 为空、为 `0`，或为 `108314295234`：自营订单；
- 其他达人 ID：达人订单。

### 5.2 是否在直播期间

- 自营订单，且订单提交时间在直播开始、结束时间之间：自营上播；
- 自营订单，但没有匹配到直播场次：自营非上播；
- 达人订单不参与自营上播判断。
- 直播明细文件没有现成 `live_id` 时，系统按“主播抖音号 + 直播开始时间”生成稳定场次编号；
- 同一订单同时匹配多场直播：标记“直播冲突”，不自动指定场次；
- 订单提交时间为空：标记“缺少下单时间”，不判断上播。

### 5.3 商品分类

商品分类暂不纳入一期关联和计算。商品 ID 直接匹配成本表；后续确认分类规则后，再增加独立分类字段和汇总维度。

### 5.4 直播期间与直播月份

- **直播期间**：订单时间落在某一场直播内；
- **直播月份**：业务将整个月定义为直播月，用于自营、非 APP、非日历订单的按月分析和分润判断。

两者不能混用。哪些月份属于直播月，需要业务维护或确认。

## 6. 商品、成本和金额

### 6.1 商品匹配

1. 去除商品 ID 中的空格；
2. 优先按“标准商品 ID + 商品编码”匹配商品版本；
3. 商品规格只用于辅助人工核对；
4. 商品编码为空、重复或不一致时，暂不自动匹配；
5. 匹配标识由系统生成，不要求业务人员手工拼接。

### 6.2 成本维护

- 商品单位成本由业务人员维护，一期不依赖商品分类；
- 新商品、新编码或缺成本商品进入待补充清单；
- 缺成本不能当作 0 元；
- 成本不直接覆盖旧记录，每次导入生成新的成本版本；
- 订单第一次成功匹配成本时保存成本快照，历史订单继续使用该快照；
- 新成本表只重新处理未匹配、缺成本或成本冲突的订单，已成功匹配的订单不自动改变。

### 6.3 金额

结算金额直接以抖店结算表中的平台结算结果为准。已经包含在平台结算结果中的服务费、达人佣金等不能重复扣除。

```text
订单商品成本 = 单位商品成本 × 商品数量
订单贡献利润 = 平台结算金额 - 订单商品成本
```

千川消耗一期不进入订单贡献利润。

系统同时保留订单月份和结算月份：经营分析可以按订单月份，财务结算可以按结算月份。默认汇总使用哪个月份仍需确认。

## 7. 后台功能

| 功能 | 主要内容 |
| --- | --- |
| 数据导入 | 上传订单、结算、直播、成本和千川数据，提示格式及重复问题 |
| 对账明细 | 按主订单查看结算、商品、达人、直播、成本和利润 |
| 对账汇总 | 按订单月份、结算月份、订单类型和达人汇总 |
| 问题处理 | 处理缺订单、未结算、拆分失败、缺商品信息和缺成本 |
| 商品成本维护 | 导入商品 ID、商家编码和单位成本，保留各次成本版本 |
| 基础名单维护 | 维护自营账号、重点达人、直播月份和千川账号 |
| 千川消耗 | 按账号、日期和消耗类型查看明细与汇总 |
| 结果追溯 | 从汇总结果查看原始订单、结算记录和来源文件 |

## 8. 一期暂不处理

- 售后退货表参与对账；
- 货损计算；
- 千川消耗分摊到订单、商品或直播场次；
- 平台接口自动获取数据；
- 抖店与小红书等渠道合并核算；
- 未确认的自营非直播订单分润计算。

结算表中已有的退款记录仍需保留，并按平台结算结果处理。

## 9. 开会需要确认

| 问题 | 需要确认的内容 |
| --- | --- |
| 订单表字段 | 多商品字段、分隔符、子订单号和规格字段如何对应 |
| 结算表去重 | 哪个字段可以唯一识别一条结算记录 |
| 自营账号 | `108314295234` 是否是唯一特殊官方账号 |
| 商品分类 | 一期最终需要哪些分类 |
| 成本组成 | 单位成本包括哪些项目，现有公式是否沿用 |
| 编码异常 | 是否允许“商品 ID + 规格”作为备用匹配方式 |
| 统计月份 | 默认按订单月份还是结算月份，跨月结算如何展示 |
| 千川用途 | 是否需要区分 APP 投流、日历投流和非合作投流 |
| 特殊结算 | 运费、保证金和平台单独扣款如何展示 |
| 分润规则 | `1100` 元建议与 `1500/5000` 阶梯规则，哪套是最终规则 |

飞书中目前存在两套分润说法：

- `1100` 元以内算小象独有，以上属于合作；
- `1500` 元以内小象独有，`1500～5000` 按 `5:5`，`5000` 以上按 `2:8`。

确认最终规则前，系统不自动计算这部分分润。

## 10. 一期完成标准

1. 重复导入订单时不会生成重复订单；
2. 订单与结算能按订单号关联，并支持一个订单对应多条结算；
3. 多商品订单无法正确拆分时会进入问题清单；
4. 能区分自营、达人、自营上播、自营非上播和商品分类；
5. 能匹配商品成本，缺成本不按 0 元处理；
6. 能展示平台结算金额、商品成本和订单贡献利润；
7. 千川按账号和日期展示消耗，不分摊到订单；
8. 汇总结果可以追溯到原始记录。

# 抖店对账系统 Cursor 开发清单

> 目标：先做出一条最小可用闭环，再补页面和汇总。  
> 原则：先导入，后关联，再计算，最后展示。  
> 技术栈：Next.js + NestJS + PostgreSQL + Prisma。  
> 说明：一期不做商品分类，千川先独立展示，不参与订单利润；商品成本支持历史快照。

## 0. 开工前先确认

- [x] 创建项目并确认前后端本地启动方式
- [x] 确定后端框架、数据库、前端框架：NestJS、PostgreSQL、Next.js
- [x] 一期任务在请求内执行，暂不引入独立消息队列
- [x] 确认当前目录没有现成业务代码，需要创建项目骨架
- [x] 确定成本支持历史版本和订单成本快照
- [x] 从真实成本表锁定匹配字段：`商品id修复 + 商家编码`
- [x] Docker Desktop 恢复后，已验证 PostgreSQL 健康状态

当前阶段 0 已完成：项目骨架、Node 22 版本约束、前后端健康接口、Prisma、Compose、构建、测试和 lint 均已建立。前端和后端进程、PostgreSQL 均已验证可以启动。

## 1. 先建数据底座

- [x] 新建导入批次表
- [x] 新建原始记录表
- [x] 新建导入错误表
- [x] 新建订单主表
- [x] 新建订单商品明细表
- [x] 新建结算记录表
- [x] 新建商品成本表
- [x] 新建商品成本版本表
- [x] 新建订单商品成本快照表
- [x] 新建直播场次表
- [ ] 新建千川消耗表
- [ ] 新建对账结果表
- [ ] 新建异常记录表

每张表至少要保留：

- [ ] 原始文件名
- [ ] 导入批次
- [ ] 原始行号
- [ ] 创建时间
- [ ] 更新时间

## 2. 先做文件导入

### 2.1 订单导入

- [x] 读取 CSV 和 XLSX 订单表表头
- [x] 校验必填表头和“主订单编号”行数据
- [x] 保存原始订单数据、文件名、批次和原始行号
- [x] 以原始 JSON 保留订单表全部字段，订单号按字符串保存
- [x] 同一文件重复导入时不重复入库
- [x] 导入失败时返回具体行和原因

验收：

- [x] 能导入一份真实订单表
- [x] 能看到成功、失败数量和文件是否重复
- [x] 能查到原始记录

当前状态：阶段 1 已完成验收。2026-08-21 使用真实抖店订单文件 `1785757668_6ff0b514ced34b085910cffe09e37eb5szbfSSw.csv`（327 行）导入成功，30 个字段完整保留（含主订单编号、支付方式、订单状态、订单应付金额、商家收入金额等），重复导入被正确拦截（duplicate: true），原始记录可通过批次接口查询。批次 ID：c02ebbaf-52cf-4c58-9394-c7dbe9b1d05f。

### 2.2 结算导入

- [x] 读取结算表
- [x] 保存订单号修复字段
- [x] 保存结算金额、结算时间、结算单类型
- [x] 允许一单多条结算记录
- [x] 支持退款负数金额

验收：

- [x] 能导入真实结算表
- [x] 能保留所有结算记录

当前状态：2026-08-21 完成。真实结算文件 DL202608031944287010388993.csv 导入 185 行（首行字段说明自动跳过），标准化出 184 条结算记录，142 条关联到订单、42 条记入“有结算无订单”问题清单；一单多结算、退款负数（-8 元）均验证通过。批次 ID：2d1c7860-4a84-4966-9fb8-ff5d028e00d1。

### 2.3 成本导入

- [x] 读取商品成本表
- [x] 保存商品 ID、商家编码、商品名称和单位成本
- [x] 每次导入生成独立成本版本，不覆盖其他版本
- [x] 成本表导入后直接生成可用版本
- [x] 新版本不覆盖旧版本
- [x] 保留成本来源批次和原始行

验收：

- [x] 能导入真实成本表
- [x] 能按“商品 ID + 商家编码”匹配成本

当前状态：2026-08-24 完成成本版本导入和真实数据验收。真实成本表 740 条原始记录中，过滤 2 条无商品标识的说明或不完整记录，保存 738 条有效成本记录。每次导入生成独立版本，新表用于未匹配、缺成本或成本冲突的订单，不覆盖已成功匹配的历史成本。

### 2.4 直播导入

- [x] 读取直播列表
- [x] 保存 live_id、开始时间、结束时间
- [x] 保存直播名称或场次标识

验收：

- [x] 能通过订单详情查看匹配的直播场次

### 2.5 千川导入

- [ ] 读取千川消耗表
- [ ] 按账号 + 日期保存
- [ ] 先只做展示，不分摊到订单

验收：

- [ ] 能按账号和日期查到消耗明细

## 3. 做订单标准化

- [x] 将订单表拆成主订单和商品明细
- [x] 一个主订单下允许多个商品明细
- [x] 保留原始订单字段，不覆盖
- [x] 建立订单详情查询接口

验收：

- [x] 任意订单都能看到完整商品明细

当前状态：2026-08-21 完成。新增 shop_order、order_item、order_issue 三张表；POST /orders/standardize/:batchId 幂等；真实批次 327 订单、342 明细、12 条多商品问题记录（单行数量金额留空待人工核对）。

2026-08-26 拆分规则优化：对齐的多商品拆分中，若商品数量合计等于拆分行数（每行必然 1 件），自动回填单行数量为 1，不再生成“商品拆分待确认”问题；单行金额仍留空（合计金额无法可靠分摊）。真实批次 12 条多商品记录中 11 条属于该可推导情况（2026-08-21 已入库的历史数据不受影响，需回填或重新标准化才生效）。

2026-08-26 存量回填完成：备份后清理真实订单批次（c02ebbaf）的旧标准化数据，重跑绑定的 12 个对账任务（全部 COMPLETED）。回填结果：327 订单 / 342 明细完整重建，成本快照 342 条全覆盖；“商品拆分待确认”由 12 条降为 1 条，仅剩 6928107630229159882（5 件 2 行，真正歧义需人工核对）；其余 11 个多商品订单每行数量已回填为 1。备份文件：.backups/reconciliation-before-backfill-20260826.dump。

## 4. 做订单和结算关联

- [x] 按“订单号修复”关联到订单主订单编号
- [x] 不允许按 Excel 行号关联
- [x] 支持一单多结算
- [x] 标记有订单无结算（按订单批次扫描）
- [x] 标记有结算无订单
- [x] 标记跨期结算（下单时间 vs 结算时间跨月）

计算结果：

- [x] 订单总结算金额 = 该订单全部结算金额之和

验收：

- [x] 任意订单都能看到对应结算记录
- [x] 异常订单能单独筛出来

当前状态：2026-08-24 完成主体关联、两类订单清扫和跨期标记。标准化结算时可通过 `orderBatchId` 指定对应的订单批次，避免把其他批次或测试数据误报。真实验收结果：327 个订单、184 条结算，142 条关联成功，42 条结算无订单，189 个订单本批次无结算，42 条跨期结算。

## 5. 做商品和成本匹配

- [x] 按订单“商品 ID + 商家编码”关联成本表“商品id修复 + 商家编码”
- [x] 未匹配、缺成本或成本冲突的订单使用本次导入的成本版本
- [x] 将匹配结果复制到订单商品成本快照
- [x] 商家编码参与匹配，避免同一商品 ID 下不同销售编码误用成本
- [ ] 商品规格只做人工核对
- [x] 找不到成本时标记“缺成本”
- [x] 同一匹配键存在多个成本值时标记“成本冲突”
- [x] 缺成本和成本冲突不能按 0 处理
- [x] 成本更新后，历史订单继续使用原成本快照
- [x] 缺成本和成本冲突可以由新成本表重新匹配
- [ ] 支持对指定范围订单按当前规则重算，并生成新的计算版本

计算结果：

- [x] 单项商品成本 = 单位成本 × 商品数量
- [x] 订单内全部商品均唯一匹配时，订单总成本 = 各商品成本之和
- [x] 有结算且成本完整时，将订单总成本用于利润计算

验收：

- [x] 匹配成功的订单商品能展示成本快照及来源版本
- [x] 缺成本和成本冲突都有明确状态

当前状态：真实订单批次共 342 个商品明细，其中 231 个唯一匹配，90 个缺成本，21 个成本冲突。三个数量合计覆盖全部商品明细。后续导入新成本表时，已有成本快照的历史订单保持不变。

## 6. 做订单类型和直播判断

- [x] 根据达人 ID 判断自营或达人
- [x] 根据订单提交时间匹配直播时间
- [x] 保存 live_id
- [x] 记录是否上播
- [x] 重叠直播标记“直播冲突”并进入问题清单
- [x] 订单提交时间为空标记“缺少下单时间”并进入问题清单

验收：

- [x] 任意订单都能看到订单类型和直播归属

当前状态：阶段 6 已完成。直播导入接口为 `POST /imports/live-sessions`，标准化接口为 `POST /live-sessions/standardize/:batchId?orderBatchId=<订单批次 ID>`。真实直播文件 `直播明细_自营账号_20260701_20260731.xlsx` 共 10 场，已匹配真实 327 条订单：自营上播 131 条、自营非上播 46 条、达人订单 150 条，冲突和时间缺失均为 0。

## 7. 做金额和利润计算

- [x] 使用结算表金额，不用支付金额代替
- [x] 退款金额按负数参与合计
- [x] 成本完整且已有结算时计算订单利润
- [x] 千川消耗不进订单利润

公式：

```text
订单总结算金额 = 关联结算金额之和
订单总成本 = 商品单位成本 × 数量之和
订单利润 = 订单总结算金额 - 订单总成本
```

验收：

- [x] 任意订单都能查看结算金额、成本、利润；缺少结算或成本不完整时显示“暂不可计算”

当前状态：订单工作台列表已直接展示总结算金额、总成本和利润；没有结算记录的订单不显示 `0.00`，成本缺失或冲突时不计算利润。订单详情仍可查看全部结算记录和商品成本快照。

## 8. 做异常清单

- [x] 记录有订单无结算
- [x] 记录有结算无订单
- [ ] 记录商品 ID 缺失
- [ ] 记录商品无法匹配
- [x] 记录商品缺成本和成本冲突
- [x] 记录重复导入
- [x] 记录字段缺失和格式错误

验收：

- [x] 异常能单独查
- [x] 异常能定位到原始行

## 9. 做查询页面

- [x] 导入页面
- [x] 导入结果页
- [x] 对账明细页
- [x] 订单详情页
- [x] 订单归属和直播状态展示及筛选
- [x] 汇总页
- [ ] 千川消耗页

当前状态：已新增“订单工作台 / 数据导入”视图切换。数据导入页支持订单、结算、商品成本和直播明细四类 CSV/XLSX 文件，上传后展示批次状态、总行数、成功数、失败数、重复状态和批次 ID。上传与标准化分步执行；结算、成本和直播处理前必须选择目标订单批次。2026-08-24 已使用 1 条独立测试订单完成浏览器上传和标准化验证，返回 1 个订单、1 个商品明细、0 个问题。

订单详情页必须能看到：

- [x] 订单信息
- [x] 商品明细
- [x] 关联结算记录
- [x] 总结算金额
- [x] 总成本
- [x] 利润
- [x] 异常原因
- [x] 来源文件

## 10. 最终验收

- [x] 导入一份真实订单表
- [x] 导入一份真实结算表
- [x] 导入一份真实成本表
- [x] 导入一份真实直播表
- [x] 跑出一批完整对账结果（订单、结算、成本和直播归属）
- [x] 能打开任意订单看当前已实现链路
- [x] 能看到异常清单
- [x] 能看到汇总结果

## 11. 现在最先做什么

按这个顺序继续：

1. ~~从飞书导出一份真实抖店订单文件，完成阶段 1 验收~~（已完成 2026-08-21）
2. ~~建订单主表和订单商品明细表，将原始记录标准化~~（已完成 2026-08-21）
3. ~~做结算导入~~（已完成 2026-08-21）
4. ~~做订单和结算关联~~（已完成 2026-08-24）
5. ~~做历史成本匹配和订单成本快照~~（已完成 2026-08-24）
6. ~~做利润计算和异常清单~~（已完成当前订单、结算、成本范围）
7. ~~做订单明细和详情页面~~（已完成基础版本）
8. ~~做直播场次导入和订单直播归属~~（已完成 2026-08-24）
9. ~~做四类数据导入和结果页面~~（已完成 2026-08-24）
10. ~~做对账汇总页面~~（已完成）
11. ~~做按月份绑定批次的对账任务~~（已完成）
12. 下一步做结算明细汇总和订单/结算/异常导出，再做千川消耗独立导入展示

## 12. 给 Cursor 的第一条指令

将下面内容复制给 Cursor：

```text
请先阅读项目中的：

1. 抖店对账系统逐步处理逻辑.md
2. 对账系统一期数据与对账逻辑技术方案.md
3. Cursor开发清单.md

先不要写前端页面，也不要一次性实现全部功能。

请先完成以下工作：

1. 检查当前项目的技术栈和启动方式；
2. 找出已有的入口文件、数据库配置和测试目录；
3. 根据开发清单设计第一版数据表；
4. 只实现导入批次表、原始订单表和订单导入功能；
5. 支持读取 Excel 或 CSV 文件；
6. 校验必填字段；
7. 保存原始字段、原始行号、文件名和导入批次；
8. 支持重复导入检测；
9. 为导入功能补充基础测试。

开始编码前，请先输出：

1. 当前项目结构；
2. 你准备修改或新增的文件；
3. 数据表设计；
4. 实现步骤；
5. 需要我确认的风险。

不要修改需求文档，不要删除现有代码，不要执行 git commit。
```

## 13. 每次开发后的 Cursor 指令

每完成一个阶段，再让 Cursor 执行：

```text
请根据 Cursor开发清单.md，只实现当前阶段。

要求：

1. 先阅读现有代码；
2. 不修改无关模块；
3. 先说明准备修改哪些文件；
4. 完成后运行相关测试；
5. 汇报已完成内容、测试结果和剩余问题；
6. 不执行 git commit。
```
